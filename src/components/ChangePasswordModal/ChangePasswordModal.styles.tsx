import {Text} from 'native-base';
import {TouchableOpacity} from 'react-native';
import styled from 'styled-components';

export const ForgotLinkWrapper = styled(TouchableOpacity)`
  margin-top: -15px;
  margin-bottom: 15px;
`;
export const ForgotLinkText = styled(Text)`
  color: #f36d32;
  font-size: 12px;
`;
