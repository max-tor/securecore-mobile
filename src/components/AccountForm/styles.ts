import {View} from 'native-base';
import styled from 'styled-components';

export const StyledAccountForm = styled(View)`
  flex: 1;
  background-color: #fff;
  padding-left: 16px;
  padding-right: 16px;
`;
