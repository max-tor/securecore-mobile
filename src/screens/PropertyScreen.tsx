import React from 'react';

import {Property} from '@/components/Property/Property';
import {MainLayout} from '@/layouts';

export const PropertyScreen = () => (
  <MainLayout title="Property">
    <Property />
  </MainLayout>
);
